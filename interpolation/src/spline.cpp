#include <interpolation/spline.h>

SplineFit::SplineFit(int num, int deg)
{
    n = num;
    degree = deg;
}

std::vector<Pose> SplineFit::interpolate(std::vector<Pose> pts)
{
    //TODO implement the interpolation
    return path;
}
